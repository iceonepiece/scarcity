#pragma once

#include <string>
#include <vector>
#include <unordered_map>
#include <filesystem>
#include <array>

/*
constexpr int MAX_COLLISION_LAYERS = 16;

class CollisionLayerManager
{
public:
	inline const std::array<std::string, MAX_COLLISION_LAYERS>& GetLayers() { return m_layers; }

private:
	std::array<std::string, MAX_COLLISION_LAYERS> m_layers;
};
*/